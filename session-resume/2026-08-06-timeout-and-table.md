# 2026-08-06 — 직업 10종 완결, 상류 대기 15초, 고발 화면 정리

> 일정: 마감까지 D-4
> 작업자: A
> 커밋: `3b25f7a` … `7ec9ee0` (브랜치 `feat/content-broker-human-only`, **아직 push 안 됨**)

## 완료

### 직업 10종 전부 살았다

검시관 · 약제사 · 순사 · 밀정 · 사진사 · 신문기자 · 변호사 · 협잡꾼 · 전화교환수 · 정보상.

- 정보상은 **사람 좌석 전용**이다(`humanOnly: true`, `src/content/roles.ts`).
  LLM은 남의 손패를 모르므로 밀담의 진위를 판정할 수 없다. 대신 말한 쪽이 스스로
  신고하고(`truthful`), 그 값은 `ParleyRecord`에 남기지 않는다 — 정보상만 본다.
- 사람 전용 직업이 있으므로 `assignRoles`는 **사람이 먼저 뽑는다.** 좌석 순서대로 돌면
  AI가 건너뛴 그 직업이 사람 차례 전에 지나가 아무에게도 가지 않는다.

### 상류 대기 시간 25초 → 15초 (`docs/decisions/008`)

배포된 프록시에 직접 39건을 넣어 쟀다.

| | 결과 |
|---|---|
| 정상 응답 (suggest·refute·challenge·accuse·parley 전부) | 4~8초 |
| 8라운드치 기록을 실은 호출 | 4.2~6.6초 — 길어도 느려지지 않는다 |
| 실패 | 39건 중 2건, 둘 다 25.5초에 504 |
| 8~25초 구간 | **응답 0건** |

느린 것이 아니라, 라운드가 병렬로 걷으므로 낙오 한 건이 나머지를 25초 세워두고 있었다.
빈 구간 안(15초)으로 내리면 살아 돌아올 응답을 자르지 않고 죽은 호출만 10초 일찍 끊는다.
프론트도 20초로 함께 내렸다 — **프록시가 더 짧다는 순서가 깨지면 실패 code가 사라진다.**

### 고친 버그 셋

- `src/ai/flow.ts` — 제안 밖 카드로 낸 AI 반증을 「넘김」으로 좁히면서 **대사는 남겼다.**
  기록은 「넘김」인데 좌석은 "그 칼은 내가 쥐고 있소"라고 말했다. `legalSpoken`이 둘을 한 쌍으로 다룬다.
- `workers/src/schema.ts` — 엔진 `Finding`에 `'parley'`가 늘었는데 화이트리스트는 3종이었다.
  지금은 경로가 막혀 있지만 열리면 그 판의 **모든** LLM 호출이 400으로 죽는다.
- `src/components/Table.tsx` — 마지막 라운드에서는 `nextRound`도 `finish`도 `round`를 올리지 않아
  지난 제안이 고발·판결 화면까지 상에 남았다. 최종 고발을 고르는 자리에 남의 제안이 깔려 있었다.

## 미완 · 다음 액션

**다음에 여기부터:** 배포. 코드는 다 됐고 확인만 안 됐다.

- [ ] **워커를 먼저 배포한다** — `npm run worker:deploy`.
      순서를 뒤집으면 옛 워커가 `names`를 거부한다. 실제로 확인했다:
      `{"ok":false,"code":"invalid_request","message":"body: 허용되지 않은 필드 'names'"}`
      그러면 새 프론트가 전부 폴백만 보게 된다.
- [ ] 브랜치를 `main`에 `--no-ff` 병합 후 push (= 배포)
- [ ] 배포 링크에서 확인 — 아래 「배포본에서 볼 것」
- [ ] `local/CLAUDE.md`의 확인 질문지 갱신

### 배포본에서 볼 것

1. 직업 10종이 실제로 발동하는가. 특히 정보상(사람 자리에만 배정)과 전화교환수(밀담 2회선).
2. 고발 화면에서 상 가운데가 비고 「상이 치워졌다 — 이제 이름을 대야 한다」가 뜨는가.
   판결문이 페이드인하는 0.3~0.5초 동안 뒤로 지난 제안이 비치지 않는가.
3. 낙오가 났을 때 그 좌석만 폴백 대사로 떨어지고 **15초 안에** 라운드가 넘어가는가.

## 막힌 것

없다.

## 검증 상태

| 항목 | 상태 |
|---|---|
| `npm run typecheck` | 통과 |
| `npm run worker:typecheck` | 통과 |
| `npm run build` | 통과 |
| 테스트 | 396/396 |
| 배포 | **확인 안 됨** — 이 작업은 아직 push되지 않았다 |

## 다음 세션 첫 명령

```bash
git switch feat/content-broker-human-only
git fetch origin && git log --oneline HEAD..origin/main   # 팀원이 올린 것 확인
npm run worker:deploy                                     # 워커가 먼저다
```
